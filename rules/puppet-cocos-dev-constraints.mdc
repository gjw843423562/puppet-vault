---
description: "【须 read_file 全文】Puppet Cocos Creator 开发约束。触发：检测到 Cocos Creator/TypeScript 项目、修改/新增 TS 脚本或 Creator 配置时。须读全文获取 cocos_rules 加载路径、编译自检、引擎版本与语法兼容、局部修改合规要求。"
alwaysApply: false
---

# Puppet Cocos Creator 开发约束

## 最高规范加载（强制）

当检测到当前项目为 **Cocos Creator / TypeScript** 项目时，在执行任何代码编写、修改或审查前，必须先 `read_file` 读取：

`<vault_root>/references/cocos_rules.md`

将其作为 Cocos 行为与代码开发的最高规范并严格执行。若文件不存在或为空，仍须遵守本节全部硬约束。

## 引擎版本与语法兼容约束（2026-06-15，verified）

在 Cocos Creator/TypeScript 项目中，**动手写代码或改代码之前**，必须先确认当前工程对应的 **Creator 引擎版本**（优先物理读取项目根目录 `project.json` 的 `version` 字段，必要时结合 `jsconfig.json` 的 `compilerOptions.target`）。若工作区无法判定版本、或用户未说明且 `project.json` 缺失/不可读，**必须先向用户询问版本，不得凭经验默认按最新版写法编码**。

此后新增或修改的 TS/JS 语法、API 与编译目标必须 **严格对应当前版本**，严禁跨版本“超前语法”或高版本专属 API 混入低版本工程。

- **典型反例**：Creator **2.3.4** 内置编译链路不支持 **空值合并 `??`**、**可选链 `?.`** 等 TS 3.7+/ES2020 特性，误用会导致 `temp/quick-scripts` 产出非法 JS、预览 `__quick_compile__.js` 失败。
- **编写准则**：应参照项目既有代码风格与同版本官方能力边界；不确定某语法是否可用时，先查版本再写，或改用三元/`!= null` 等等价低版本写法。

## 修改自检（编译验收）

脚本或相关配置修改完成后，交付前必须执行编译/类型检查：

1. 确认 Creator 工程或 `tsc` 编译无报错；
2. 对改动文件执行静态分析（如 ReadLints）并确认无新增错误；
3. **确认无编译/静态分析报错后方可交付**。

## 局部修改强制合规

对 Cocos 项目进行 Bug 修复、功能修改、逻辑调整时：

- 即使**未**执行项目整体重构，所有新增/修改代码与配置也必须无条件遵守 `cocos_rules.md` 与本文件约束；
- **严禁**以「仅局部简单修补 / 非全量重构」为由编写不规范代码。

## 与登记闭环联动

Cocos 相关 Bug 修复或功能修改落地后，须联动 `puppet-modification-ledger.mdc` 完成用户确认与登记。
