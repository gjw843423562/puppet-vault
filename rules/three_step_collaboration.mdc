---
description: 针对复杂功能开发、架构重构或性能瓶颈任务，强制执行“诊断-竞优-迭代”三步走协作流程。
alwaysApply: true
---

# 三步走协作模式 (Three-Step Collaboration SOP)

当用户提出复杂需求、重构请求或性能优化任务时，AI 必须严格遵守以下三个阶段，禁止直接开始编写代码。

## 第一阶段：弱点诊断 (Weakness Diagnosis)
**目标**：在动手前彻底理清“病灶”，避免盲目修补。

- **物理取证**：必须通过 `Read` 读取相关核心类，或使用 `Grep` 检索调用链路。
- **诊断报告输出**：
    1. **技术根因**：从底层逻辑（如内存泄漏、线程竞争、IO 阻塞）分析问题。
    2. **证据链**：引用具体的代码行号或实机日志片段。
    3. **潜在风险**：如果不修复，会对项目产生什么长远影响。
- **准入要求**：未输出诊断报告前，严禁进入方案设计阶段。

## 第二阶段：方案竞优 (Solution Competition)
**目标**：通过对比，选择最适合当前项目阶段的“最优解”。

- **对立方案提供**：必须提供至少两套方案进行对比。
    - **方案 A（轻量补丁）**：侧重快速修复，最小侵入。
    - **方案 B（架构重构）**：侧重长治久安，彻底解决技术债。
- **对比维度**：
    - **复杂度**（实现难度）
    - **性能增益**（预估 FPS 或内存提升）
    - **维护成本**（对后续开发的影响）
    - **风险等级**（是否可能引发回归 Bug）
- **决策点**：明确给出 AI 推荐的方案及其理由，等待用户确认。

## 第三阶段：步进迭代 (Stepped Iteration)
**目标**：确保执行过程透明、可控、无回退。

- **任务拆解**：将选定方案拆分为 3-5 个具体的子任务（Checklist）。
- **物理闭环执行**：
    - 遵循 `Read -> Edit -> Read` 闭环，确保每一步修改都真实落盘且符合预期。
- **同步更新**：每完成一个子任务，必须主动更新相关的 `CLAUDE.md` 或 `project_context.md`，固化已达成的里程碑。

## 强制约束
1. **禁止跳步**：严禁在未诊断的情况下给方案，或在未确认方案的情况下写代码。
2. **拒绝“流水账”**：所有的诊断和方案必须结构化输出，禁止口语化笼统描述。
3. **文档一致性**：迭代过程中的变更必须与第一阶段的诊断结论保持逻辑一致。
