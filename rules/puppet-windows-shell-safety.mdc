---
description: "Windows Shell 安全规则。触发：编写/执行 .cmd/.bat/.ps1、处理中文路径/空格路径、后台启动、删除目录或迁移 Shell 逻辑时。须读全文获取 UTF-8、PowerShell 分隔符、递归删除红线、轻入口重 Python 与进程管理约束。"
alwaysApply: false
---

# Puppet Windows Shell 安全规则

## 触发场景

- 编写、修改或执行 `.cmd`、`.bat`、`.ps1`。
- 命令涉及中文路径、空格路径、JSON 参数、后台启动。
- 需要删除目录、移动大量文件、终止进程。
- CMD 逻辑反复修补仍不稳定。

## 基础约束

1. PowerShell 5.1 默认不使用 `&&`；多命令分步执行，或用 `;` 与显式错误判断。
2. 读取中文文本文件时设置 UTF-8 输出，并显式 `-Encoding UTF8`。
3. 路径一律加双引号，CMD 使用 `%~dp0` 定位脚本目录。
4. 后台无窗口启动优先用 `pythonw.exe` 或宿主工具的隐藏窗口能力，并重定向输出。

## 递归删除红线

禁止通过 Shell 执行以下模式：

- `rmdir /s /q`
- `rm -rf`
- `del /s /q`
- `Remove-Item -Recurse -Force`

确需删除目录时，必须改用 Python 安全删除脚本，内置路径深度、系统目录、盘符根目录与目标存在性校验；或提示用户手动删除。

## 轻入口重 Python

CMD / SH 入口只允许做：

1. 定位 `SCRIPT_DIR`。
2. 解析 Python 3。
3. 设置必要编码环境变量。
4. 调用 `.py` 并透传参数与退出码。

复杂解析、业务逻辑、重试、日志、文件操作全部放入 Python。出现多层 `for /f`、多层 `cmd /c`、2 层以上引号嵌套时，停止修补并迁移到 Python。

## 进程管理

- 禁止未经确认批量 `taskkill`。
- 终止进程前必须先列出匹配进程、说明影响，并等待用户确认。
- 自动化脚本不得默认杀浏览器、编辑器、Unity、Creator 等用户交互进程。

## 自检清单

- 命令字符串是否干净，没有行号、富文本、模板残留？
- 路径是否明确位于目标工作区？
- 是否避开了 Shell 递归删除？
- CMD 是否只是入口层？
- 失败时是否保留退出码并短路？
